package org.sitmun.administration.controller.dto;

/** Top-level image upload constraints exposed to admin app consumers. */
public record AdminImageUploadConfigurationDto(TreeImageUploadConfigurationDto tree) {}
